// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             (unknown)
// source: harness/v1/harness.proto

package harnessv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	HarnessService_RunTask_FullMethodName = "/stirrup.harness.v1.HarnessService/RunTask"
)

// HarnessServiceClient is the client API for HarnessService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// HarnessService is the bidirectional streaming interface between the harness
// (client) and the control plane (server). The harness connects outbound and
// opens a single bidi stream for the lifetime of a task run.
//
// Lifecycle:
//  1. Harness connects and opens a RunTask stream.
//  2. Harness sends a HarnessEvent with type "ready" (includes harness_version).
//  3. Control plane sends a ControlEvent with type "task_assignment" containing
//     a RunConfig in the task field.
//  4. Harness streams HarnessEvents as it works (text_delta, tool_call,
//     tool_result, heartbeat, permission_request, warning).
//  5. Control plane sends ControlEvents as needed (user_response,
//     permission_response, cancel).
//  6. Harness sends a final HarnessEvent with type "done" (includes trace)
//     or "error", then the stream closes.
type HarnessServiceClient interface {
	// RunTask opens a bidirectional stream. The control plane sends a
	// TaskAssignment as the first ControlEvent; the harness streams
	// HarnessEvents back as it works, and reads ControlEvents for user
	// responses, permission decisions, and cancellation signals.
	RunTask(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[HarnessEvent, ControlEvent], error)
}

type harnessServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewHarnessServiceClient(cc grpc.ClientConnInterface) HarnessServiceClient {
	return &harnessServiceClient{cc}
}

func (c *harnessServiceClient) RunTask(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[HarnessEvent, ControlEvent], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &HarnessService_ServiceDesc.Streams[0], HarnessService_RunTask_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[HarnessEvent, ControlEvent]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type HarnessService_RunTaskClient = grpc.BidiStreamingClient[HarnessEvent, ControlEvent]

// HarnessServiceServer is the server API for HarnessService service.
// All implementations must embed UnimplementedHarnessServiceServer
// for forward compatibility.
//
// HarnessService is the bidirectional streaming interface between the harness
// (client) and the control plane (server). The harness connects outbound and
// opens a single bidi stream for the lifetime of a task run.
//
// Lifecycle:
//  1. Harness connects and opens a RunTask stream.
//  2. Harness sends a HarnessEvent with type "ready" (includes harness_version).
//  3. Control plane sends a ControlEvent with type "task_assignment" containing
//     a RunConfig in the task field.
//  4. Harness streams HarnessEvents as it works (text_delta, tool_call,
//     tool_result, heartbeat, permission_request, warning).
//  5. Control plane sends ControlEvents as needed (user_response,
//     permission_response, cancel).
//  6. Harness sends a final HarnessEvent with type "done" (includes trace)
//     or "error", then the stream closes.
type HarnessServiceServer interface {
	// RunTask opens a bidirectional stream. The control plane sends a
	// TaskAssignment as the first ControlEvent; the harness streams
	// HarnessEvents back as it works, and reads ControlEvents for user
	// responses, permission decisions, and cancellation signals.
	RunTask(grpc.BidiStreamingServer[HarnessEvent, ControlEvent]) error
	mustEmbedUnimplementedHarnessServiceServer()
}

// UnimplementedHarnessServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedHarnessServiceServer struct{}

func (UnimplementedHarnessServiceServer) RunTask(grpc.BidiStreamingServer[HarnessEvent, ControlEvent]) error {
	return status.Error(codes.Unimplemented, "method RunTask not implemented")
}
func (UnimplementedHarnessServiceServer) mustEmbedUnimplementedHarnessServiceServer() {}
func (UnimplementedHarnessServiceServer) testEmbeddedByValue()                        {}

// UnsafeHarnessServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to HarnessServiceServer will
// result in compilation errors.
type UnsafeHarnessServiceServer interface {
	mustEmbedUnimplementedHarnessServiceServer()
}

func RegisterHarnessServiceServer(s grpc.ServiceRegistrar, srv HarnessServiceServer) {
	// If the following call panics, it indicates UnimplementedHarnessServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&HarnessService_ServiceDesc, srv)
}

func _HarnessService_RunTask_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(HarnessServiceServer).RunTask(&grpc.GenericServerStream[HarnessEvent, ControlEvent]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type HarnessService_RunTaskServer = grpc.BidiStreamingServer[HarnessEvent, ControlEvent]

// HarnessService_ServiceDesc is the grpc.ServiceDesc for HarnessService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var HarnessService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "stirrup.harness.v1.HarnessService",
	HandlerType: (*HarnessServiceServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "RunTask",
			Handler:       _HarnessService_RunTask_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "harness/v1/harness.proto",
}
